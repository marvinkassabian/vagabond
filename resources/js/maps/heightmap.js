(function() {
  "use strict";

  VAGABOND.namespace("VAGABOND.MAPS");

  VAGABOND.MAPS = (function(module) {

    var HeightMap = {};

    HeightMap.init = function() {
    };

    HeightMap.renderTo = function() {
    };

    module.HeightMap = HeightMap;

    return module;

  })(VAGABOND.MAPS);
})();
